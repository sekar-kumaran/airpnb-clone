name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Pipeline
        run: |
          echo "Triggering Jenkins pipeline..."
          # Replace the URL and token with your actual Jenkins server details
          # curl -X POST "http://your-jenkins-server.com/job/airbnb-clone/buildWithParameters?token=GITHUB_TRIGGER_TOKEN"
          echo "Jenkins triggered successfully (mocked)."

